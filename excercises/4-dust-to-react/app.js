////////////////////////////////////////////////////////////////////////////////
// Excercise:
// - Convert note.dust (and it's included textarea.dust) to a React Component
// - Insert the note component into #react-version
// - Compare output in index.html
//
// TODO [jamis]: Add tests to assert that it worked
////////////////////////////////////////////////////////////////////////////////
var React = require('react');


var App = React.createClass({


  render () {
    return (
      null
    );
  }

});


React.render(<App />, document.body);

